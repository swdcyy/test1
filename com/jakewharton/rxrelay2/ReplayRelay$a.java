package com.jakewharton.rxrelay2.ReplayRelay$a;
import com.jakewharton.rxrelay2.ReplayRelay$ReplayDisposable;
import java.lang.Object;

public interface abstract ReplayRelay$a	// class@0006d5
{

    void a(ReplayRelay$ReplayDisposable p0);
    void add(Object p0);
    Object getValue();
    Object[] getValues(Object[] p0);
    int size();
}
